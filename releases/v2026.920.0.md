# v2026.920.0

## Fixes

- Memory distillation gate: fix exception flood by gating distillation on user-authored text length instead of total text length (OS-7620)
- Cross-issue write gate: let heartbeat runs write to the issue they checked out (fixes agent 403 deadlock on same-issue writes) — OS-7477, OS-6847
- Fail open on cross-issue gate errors to prevent test failures
- Fix logger.warn signature in cross-issue gate error handler
- Cascade delete run_log_chunks on company removal

## Features

- Full statelessness conversion — eliminate Railway volume deps (BUY-18587)
- Add log_chunks tables for postgres run log store (BUY-18587)
- Redact secret_ref.secretId from agent runtimeConfig in /api/agents/me and /api/agents/:id (OS-4619)
- Remove NODE_AUTH_TOKEN — use OIDC trusted publishing for npm publish access (BUY-63611)
