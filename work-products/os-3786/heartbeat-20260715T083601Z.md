# OS-3786 heartbeat

At: 2026-07-15T08:36:15.000Z
Issue: OS-3786 [Sage heartbeat]
Latest comment: none (0/0)

- Acknowledged latest payload: heartbeat is active in `in_progress` with no pending comments and explicit instruction to perform quick periodic checks.
- Ran `scripts/smoke-probe-8os.sh --attempts 2 --json` against production and web-only checks.
- Probe summary (json mode):
  - PASS: 15/17 overall (OS-1144 `/api/auth/google` returns 307; OS-1208 affiliates/terms 200; waitlist/orchestrator/redirect/1092 probes pass)
  - FAIL: 2
    - OS-1199 `/api/auth/google` Location is Google consent: failed (`Location: https://0.0.0.0:3000/login?error=Google%20sign-in%20is%20temporarily%20unavailable`, expected Google consent URL).
    - OS-1173 `/coming-soon` 200: failed (`/coming-soon` currently 307 -> `/signup`).
- Verified with direct `curl -I` probes for `/coming-soon` and `/api/auth/google` returning same failure signals.
- Posted detailed status comment to Paperclip via API and set issue disposition to `done`.
- No code changes made in this heartbeat (no repo source edits).
- Next action recommendation remains unchanged:
  - OS-972 owner path likely needs environment + routing correction (`NEXT_PUBLIC_SITE_URL` fallback host and coming-soon route serving)
