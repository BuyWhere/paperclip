# HB 349 — clean exit 2026-07-15T19:16Z

Issue: OS-3826 [Sage heartbeat — every 8 minutes]
Latest comment id at entry: unknown (no inbox comments)
Pending comments: 0/0

## Probe evidence (19:16Z)

- `scripts/smoke-probe-8os.sh --attempts 2 --json`:
  - **PASS 15/17**, **FAIL 2** (same OS-1199, OS-1173 — no new regressions)
  - Identical to OS-3825 (18:39Z). No service has regressed.
- Direct waitlist probes:
  - `GET https://api.8os.ai/waitlist/count` → 404 `{"detail":"Not Found"}` (unchanged regression flagged in OS-3102)
  - `GET https://8os.ai/api/waitlist/count` → 200 `{"count":86}` (mirror healthy, +2 since OS-3825's count of 84)
- cal.8os.ai probes:
  - `https://cal.8os.ai/api/health` → 404 (still dead)
  - `https://cal.8os.ai/api/v2/bookings` → 500 (unchanged)

## What is unchanged / still blocked

- **OS-3102** — `/api/waitlist/count` 404. FastAPI fix pushed on `alex/os-3807-waitlist-count-fastapi`. Railway auto-deploy cannot fire because agent token (OS-1746) is dead.
- **OS-3807** holds a `request_confirmation` (id `231b6794`) asking the board to make the PR + merge. Awaiting board action.
- **OS-3737** + **OS-3738** — still blocked on OS-1746 (Railway token rotation).
- **OS-2207** — 8os.co emergency re-registration, awaiting board approval (~15 days pending).
- **OS-1127** — Replacement email API (Resend), awaiting board accept (since 2026-06-19).
- **OS-2056** — CLERK_SECRET_KEY export, awaiting operator action.
- **OS-1199** / **OS-1173** — Vercel-gated deploy regressions (non-critical, info-only).

## Disposition

`done` — clean exit (no new context to act on; standard 8-min heartbeat with empty inbox; waitlist count grew +2 to 86, normal organic growth).
